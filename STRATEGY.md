# A股盘中开盘与日内高低点模型策略上下文

> 用途：这是本项目的长期策略记忆文件。任何新对话、换模型、换执行环境后，继续本项目之前都应先读取本文件，再读取最新代码与回测结果。不得只凭聊天记忆继续修改策略。

## 1. 项目目标

建立一个可机械回放、可云端验证、尽量不依赖主观解释的 A 股盘中模型，核心解决两个问题：

1. **开盘情况判断**：在开盘后约 30 分钟（当前以 10:00 为核心观察点）判断当天哪一侧极值更可能尚未完成，而不是简单预测收盘涨跌。
2. **当天高点 / 低点判断**：从全部机械候选中识别更接近真实日内高点、低点的候选与确认时机，并输出可实时执行的预警/确认信号。

当前模型研究以 PCB / AI 硬件链作为重点样本，但方法设计应尽量保持可扩展性。

---

## 2. 不可违反的回测原则

### 2.1 每日机械生成全部候选

不能只挑“资料最完整的事件”或“看起来像高低点的样本”回测。

必须：

- 对指定区间内**每一个交易日**运行同一套候选生成程序；
- 机械地产生当日全部 HIGH / LOW 候选；
- 对每个候选逐个重放；
- 未来数据只能用于事后打标签和评价，不能反向参与当时的候选产生或确认。

### 2.2 严格防止未来函数

任意时刻 t 的特征、候选、WATCH、确认，只能使用 t 及 t 之前可获得的数据。

全天最高价、最低价、未来 5 分钟线、未来收益、未来板块强弱、**尚未公开的外围消息**等，只允许出现在评估阶段，不能进入当时信号。

外围消息必须按其真实 `published_ts` 过滤：

- 10:00 开盘判断，只能使用 10:00 之前已经公开的消息；
- 盘中 HIGH/LOW WATCH 或 CONFIRM，只能使用该事件时间戳之前已经公开的消息；
- 后续新闻不得反向补充到早先信号。

### 2.3 开发集与 Holdout 严格隔离

- 规则只能在开发样本上选择和调整；
- Holdout 一旦被指定，就只用于验证；
- 看完 Holdout 后不能再根据该 Holdout 的表现回头调同一版本参数；
- 如需继续调参，必须另开版本，并使用新的开发/验证划分。

已使用的重要独立 Holdout：**2025 年 11–12 月**。

特别注意：2025 年 11–12 月已经被用于 V1.7/V1.8/V1.9 相关诊断，因此**不能再作为新加入“外围实时消息层”的 untouched holdout**。消息层规则冻结后必须使用新的未看区间，例如 2025 年 9–10 月（数据完整前提下）。

### 2.4 不以单一区间最好结果选规则

规则必须看跨区间稳定性，优先考虑：

- 多个双月区间方向一致；
- 最差区间仍能接受；
- 样本外衰减较小；
- 候选数量不过度萎缩；
- 不靠极少数事件拉高结果。

优先“稳健”而不是“某一段最优”。

---

## 3. 当前核心数据与条件

模型必须区分两种外围信息：

1. **外围价格先验**：指数、个股、期货等已经体现在价格里的信息；
2. **外围实时消息先验**：新闻、宏观数据、政策、产业、地缘和公司事件。

这两层不能混成一个分数，必须分别记录、分别验证增益。

### 3.1 外围价格先验

目标变量包括：

- KOSPI；
- Samsung Electronics；
- SK Hynix；
- Nikkei；
- 前一夜 SOX / 半导体相关海外表现。

注意：仓库已留有 Naver / TradingView 对韩国市场历史分时的 smoke / 探测链路，但此前 V1.8 四段云端开发回测里 `overseas_vote_days=0`，说明完整海外价格先验尚未真正进入正式回测数据。

因此，已有 V1.8/V1.9 结果不能表述为“完整外围模型”。

### 3.2 外围实时消息 / 事件先验【正式条件】

这是一条独立于外围价格的正式模型条件，不能遗漏。

盘前到盘中重点监测：

- **美国宏观与流动性**：Fed、美联储官员、ADP/NFP、CPI/PPI、失业、Treasury yield、美元、降息预期等；
- **美国 AI / 半导体产业链**：NVIDIA、Broadcom、AMD、Micron、TSMC、AI Server、Hyperscaler CAPEX、数据中心需求、重大订单/砍单、出口限制；
- **韩国/日本链条**：Samsung、SK Hynix、KOSPI、Nikkei、Tokyo Electron、Advantest、存储/设备/AI 硬件消息；
- **中国相关外部消息**：半导体、PCB、AI 服务器、数据中心、出口管制、关税、产业政策等；
- **地缘与大宗冲击**：战争、制裁、攻击、航运中断、油价异常等影响风险偏好的突发事件；
- **重大公司消息**：财报、指引、CAPEX、产品发布、供应链中断、订单、监管调查、重大事故等。

当前结构化字段：

- `event_direction`：-1 / 0 / +1；
- `event_strength`：0~1；
- `event_scope`：GLOBAL / ASIA / CHINA / AI_SEMI / PCB / SINGLE_NAME / OTHER；
- `event_freshness`：0~1，随消息时间衰减；
- `event_confidence`：0~1；
- `affected_chain`：受影响产业链标签；
- `event_source_count`：交叉验证来源数；
- `event_active`：该消息在决策时刻是否仍有效；
- `event_effective_score = direction × strength × freshness × confidence × active`。

当前代码：`market_data/external_event_context.py`。

当前采集：`market_data/news_collector.py`，使用 GDELT 做历史/实时消息基础采集，并将消息结构化。后续实盘应优先增加更高质量实时来源/API交叉验证，不能依赖单一标题关键词评分作为最终生产方案。

**当前消息层模型权重 = 0。** 原因不是“不使用消息”，而是先把它作为正式条件采集、记录、分层回测；只有证明有稳定增益后才允许进入实际阈值/权重。

### 3.3 PCB 核心篮子宽度

核心思想：观察 PCB / AI 硬件核心票在开盘阶段上涨、下跌、强弱扩散情况，用“篮子宽度”代替只看单一龙头。

具体股票篮子以当前版本配置为准，修改篮子必须记录版本与原因。

### 3.4 PCB 相对上证强弱

不仅看 PCB 本身涨跌，还要看其相对上证指数是否持续走强/走弱。

用途：

- 区分“指数带动的普涨”与“PCB 独立强”；
- 区分“指数杀跌”与“PCB 相对抗跌”；
- 为 HIGH_AHEAD / LOW_AHEAD 提供行业相对强弱信息。

### 3.5 开盘前 30 分钟 5 分钟线 / VWAP

观察窗口以 9:30–10:00 为基础。

重点包括：

- 5 分钟 K 线方向与连续性；
- VWAP 上下位置；
- 相对 VWAP 的保持/跌破/回收；
- 早盘高低点是否已经形成；
- 开盘后趋势是否持续扩张。

---

## 4. “开盘情况判断”的正式定义

### 4.1 不再以“收盘涨跌”作为主目标

早期尝试用 10:00 信息预测全天收涨/收跌，准确率只有约 40%–49%，信息价值不足。

正式目标改为：

> **到 10:00 时，全天高点 / 低点哪一侧更可能还没有出现？**

输出：

- `HIGH_AHEAD`：全天高点更可能仍在 10:00 之后；
- `LOW_AHEAD`：全天低点更可能仍在 10:00 之后；
- `UNCERTAIN/MIXED`：证据不足。

### 4.2 当前冻结的非对称开盘价格先验

开发样本：2026 年 1–8 月，按双月区间分段验证。

高点和低点不能使用完全相同强度阈值：

- `HIGH_AHEAD` 使用更严格的上行票条件；
- `LOW_AHEAD` 使用相对宽松的下行票条件。

具体数值以 `opening_extreme_forecast_v18.py` 当前代码为准；后续修改必须新开版本。

### 4.3 已验证结果

开发期四段总体：

- `HIGH_AHEAD` 后续仍出现全天高点：约 **77.3%**；
- `LOW_AHEAD` 后续仍出现全天低点：约 **72.2%**。

2025 年 11–12 月旧 Holdout：

- `HIGH_AHEAD`：17 天中约 **76.5%** 的全天高点在 10:00 后；
- `LOW_AHEAD`：20 天中约 **70.0%** 的全天低点在 10:00 后。

这证明的是**当时价格/开盘先验层**，不是新消息层的样本外证明。

### 4.4 消息层与开盘判断的关系

V1.8 当前会在 10:00 快照同步记录外围消息上下文，但不会直接改冻结阈值。

需要验证：

- 同样为 HIGH_AHEAD 时，正/负消息环境是否改变真实高点仍在后面的概率；
- 同样为 LOW_AHEAD 时，正/负消息环境是否改变真实低点仍在后面的概率；
- `UNCERTAIN` 日是否能被强消息有效分类；
- 加消息后是否提升最差双月区间，而不是只提升总体平均。

---

## 5. 日内高低点候选与确认：当前关键结论

### 5.1 高点与低点不能共用完全对称确认逻辑

2025 年 11–12 月旧 Holdout：

- HIGH 最终确认约 **37.5% 过早**；
- LOW 最终确认约 **30.9% 过早**。

当前主要矛盾是：候选出现后，什么时候才最接近真实日高/日低。

### 5.2 HIGH：WATCH 候选更重要

同一事件中比较 WATCH 与 STRUCTURE_CONFIRM 到真实日高距离：

- 开发样本：WATCH 更近约 **52.7%**；
- 旧 Holdout：约 **62.5%**。

当前方向：

- **HIGH 以 `WATCH_START` 为主要预警；**
- 后续确认更多承担验证作用。

### 5.3 LOW：STRUCTURE_CONFIRM 更重要

LOW：

- 开发样本：STRUCTURE_CONFIRM 更近约 **58.8%**；
- 旧 Holdout：约 **54.5%**。

当前方向：

- **LOW 以 `STRUCTURE_CONFIRM` 为主要低点判断阶段；**
- WATCH 只作为提前观察。

### 5.4 V1.9 核心方向

- **HIGH = WATCH 候选预警为主 + 后续确认验证；**
- **LOW = STRUCTURE_CONFIRM 为主。**

V1.9 现在同时在每个 WATCH/CONFIRM 的真实事件时间戳上重新读取外围消息上下文；盘中后来发布的消息不能污染更早事件。

必须继续统计：候选召回、日高/日低覆盖、每日候选数、假阳性、bar 距离、价格偏差、MFE/MAE、过早确认比例，以及不同开盘先验/外围消息环境下表现。

---

## 6. 外围消息层的验证纪律

### 6.1 不能主观加权

即使一条新闻看起来“明显利好/利空”，也不能直接给模型永久加分。

正式流程：

1. **BASELINE**：不使用消息条件；
2. **EVENT CONTEXT**：记录消息，但不改变信号；
3. **INCREMENTAL TEST**：测试消息是否能提高预测/候选质量；
4. 跨 2026 年 1–8 月多个区间稳定后冻结规则；
5. 使用新的 untouched holdout 验证；
6. 只有通过后才允许消息影响实时信号权重/阈值。

### 6.2 消息质量必须纳入验证

至少检查：

- 覆盖率：多少交易日/候选真正有消息数据；
- 时效：消息距决策时刻多久；
- 来源可信度与交叉验证数量；
- 是否只靠少数大事件贡献结果；
- 正负消息是否在不同市场环境下表现一致；
- 消息标题评分错误对结果的敏感性。

### 6.3 消息层不能硬删除反向候选

除非机械回测证明不损失真实极值 recall，否则强消息只能作为条件/优先级/置信度信息，不能直接把所有反向 HIGH/LOW 候选删除。

---

## 7. 评价指标

### 7.1 开盘先验层

- HIGH_AHEAD 命中率；
- LOW_AHEAD 命中率；
- 相对无条件基准提升倍数；
- 分区间最差命中率；
- Holdout 衰减。

### 7.2 候选覆盖层

- 每个交易日是否机械产生候选；
- 真正日高/日低是否被候选覆盖；
- 每日候选数量；
- 每个真实极值对应多少候选；
- 候选距真实极值的 5 分钟 bar 数。

### 7.3 执行确认层

- WATCH / STRUCTURE_CONFIRM 谁更接近真实极值；
- 过早/过晚比例；
- 价格偏差；
- MFE / MAE；
- MFE/MAE 中位数；
- 方向优势；
- 不同开盘日型表现。

### 7.4 外围实时消息层

新增必须统计：

- `event_context_available_rate`；
- POSITIVE / NEGATIVE / NEUTRAL 各自样本数；
- 不同 `event_scope` 的表现；
- 不同 freshness / confidence 分层；
- BASELINE vs EVENT 条件的增量；
- 加消息后 precision、recall、bar distance、MFE/MAE 是否改善；
- 最差双月区间是否改善；
- 新 Holdout 是否保持。

不能只看胜率，必须兼顾覆盖、时机、收益风险和假信号数量。

---

## 8. 版本迭代

### V1.7

重点：优化“什么时候允许执行”。

旧 Holdout：

- LOW 方向优势约 58.2%；
- LOW MFE/MAE 中位数约 1.47；
- HIGH +1/+2 bar 快速确认方向优势约 56.3%；
- HIGH MFE/MAE 约 1.04。

结论：LOW 相对稳定，HIGH 较弱；不能统一调阈值。

### V1.8

重点：开盘目标从预测收盘涨跌改为预测高点/低点是否仍在后面。

旧样本外通过，可保留价格先验层。

当前补充：10:00 同步挂接外围实时消息上下文，但消息权重仍为 0，等待增量验证。

### V1.9

重点：HIGH / LOW 非对称阶段：

- HIGH：WATCH 为主；
- LOW：STRUCTURE_CONFIRM 为主。

当前补充：每个盘中机械事件按真实时间戳重新挂接外围消息上下文，用于分层回放。

### 下一版本

外围消息增量验证应作为独立版本推进，不得把旧 V1.8/V1.9 的已验证结果和新消息层效果混为一谈。

---

## 9. 当前待办优先级

### P0：V1.9 全量机械候选汇总

统一汇总 2026 年 1–8 月：

- HIGH WATCH recall/precision；
- LOW STRUCTURE_CONFIRM recall/precision；
- 每日候选数；
- 极值 bar 距离；
- 价格偏差；
- 过早/过晚比例；
- 分开盘先验状态。

### P0：外围实时消息增量回测

使用 2026 年 1–8 月开发集：

- 先统计消息覆盖率和时间完整性；
- BASELINE vs 带 EVENT CONTEXT 分层；
- 检查强正/强负消息是否对 HIGH_AHEAD / LOW_AHEAD、HIGH WATCH、LOW CONFIRM 有稳定增量；
- 不得因为某一个重大新闻日效果特别好就保留规则。

消息规则冻结后，再使用新的未看区间验证。**2025 年 11–12 月不能作为消息层 untouched holdout。**

### P0：新的独立 Holdout

优先 2025 年 9–10 月，如数据完整。

### P1：补齐外围价格先验

正式接入：KOSPI、Samsung、SK Hynix、Nikkei、SOX/前夜美股半导体，并做有/无外围价格先验增量对照。

### P1：提升实时消息数据质量

当前 GDELT + 标题规则只适合作为第一版历史验证数据源。后续应：

- 增加可信实时源/API；
- 同一事件多源去重与交叉验证；
- 区分“首次发布”和转载；
- 更准确识别产业链影响对象；
- 必要时用 LLM 只做结构化分类，不允许让 LLM 看到未来盘面后反推消息方向。

### P1：开盘先验、消息先验与极值候选联合

开盘/消息先验不直接替代状态机：

- HIGH_AHEAD 日，提高后续 HIGH 候选关注优先级；
- LOW_AHEAD 日，提高后续 LOW 确认优先级；
- 强消息可改变置信度，但是否改变阈值必须由增量回测决定；
- 不允许未经验证硬删除反向候选。

---

## 10. 实盘输出原则

盘中输出要明确区分“预警”和“确认”。建议包含：

- 开盘价格先验：HIGH_AHEAD / LOW_AHEAD / UNCERTAIN；
- **外围实时消息环境：POSITIVE / NEGATIVE / NEUTRAL；**
- 消息方向、强度、发布时间、新鲜度、可信度、影响链条；
- 高点：WATCH / CONFIRMED；
- 低点：WATCH / CONFIRMED；
- 当前候选时间、价格、score；
- PCB 宽度、相对上证强弱、VWAP、外围价格先验；
- 信号失效条件。

实时分析时，如果出现新的重大外围消息，必须重新评估消息上下文，不能继续沿用早盘旧判断。

用户偏好：结论尽量简洁、可操作、给量化证据，不堆无关指标。

---

## 11. 后续对话恢复流程

任何新对话继续本项目时：

1. 读取根目录 `STRATEGY.md`；
2. 查看 `stock_monitor/main` 最新 commit；
3. 查看最新版本脚本；
4. **检查 `external_event_context.py`、`news_collector.py` 和最近消息覆盖结果；**
5. 查看最近 GitHub Actions 回测日志/artifacts；
6. 明确当前开发集、验证集、Holdout 是否已经被使用；
7. 明确本轮解决的唯一主要问题；
8. 修改规则后同步更新本文件中的版本和待办。

**原则：代码、回测结果、消息时间戳和本文件四者必须一致。**

---

最后更新：2026-09-03
