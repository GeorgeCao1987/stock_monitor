# A股盘中开盘与日内高低点模型策略上下文

> 用途：这是本项目的长期策略记忆文件。任何新对话、换模型、换执行环境后，继续本项目之前都应先读取本文件，再读取最新代码与回测结果。不得只凭聊天记忆继续修改策略。

## 1. 项目目标

建立一个可机械回放、可云端验证、尽量不依赖主观解释的 A 股盘中模型，核心解决两个问题：

1. **开盘情况判断**：在开盘后约 30 分钟（当前以 10:00 为核心观察点）判断当天哪一侧极值更可能尚未完成，而不是简单预测收盘涨跌。
2. **当天高点 / 低点判断**：从全部机械候选中识别更接近真实日内高点、低点的候选与确认时机，并输出可实时执行的预警/确认信号。

当前模型研究以 PCB / AI 硬件链作为重点样本，但方法设计应尽量保持可扩展性。

---

## 2. 不可违反的回测原则

### 2.1 每日机械生成全部候选

不能只挑“资料最完整的事件”或“看起来像高低点的样本”回测。

必须：

- 对指定区间内**每一个交易日**运行同一套候选生成程序；
- 机械地产生当日全部 HIGH / LOW 候选；
- 对每个候选逐个重放；
- 未来数据只能用于事后打标签和评价，不能反向参与当时的候选产生或确认。

### 2.2 严格防止未来函数

任意时刻 t 的特征、候选、WATCH、确认，只能使用 t 及 t 之前可获得的数据。

全天最高价、最低价、未来 5 分钟线、未来收益、未来板块强弱等，只允许出现在评估阶段。

### 2.3 开发集与 Holdout 严格隔离

- 规则只能在开发样本上选择和调整；
- Holdout 一旦被指定，就只用于验证；
- 看完 Holdout 后不能再根据该 Holdout 的表现回头调同一版本参数；
- 如需继续调参，必须另开版本，并使用新的开发/验证划分。

已使用的重要独立 Holdout：**2025 年 11–12 月**。

### 2.4 不以单一区间最好结果选规则

规则必须看跨区间稳定性，优先考虑：

- 多个双月区间方向一致；
- 最差区间仍能接受；
- 样本外衰减较小；
- 候选数量不过度萎缩；
- 不靠极少数事件拉高结果。

优先“稳健”而不是“某一段最优”。

---

## 3. 当前核心数据与变量

### 3.1 海外开盘先验

目标变量包括：

- KOSPI；
- Samsung Electronics；
- SK Hynix；
- Nikkei；
- 前一夜 SOX / 半导体相关海外表现。

注意：仓库已留有 Naver / TradingView 对韩国市场历史分时的 smoke / 探测链路，但在 V1.8 当前四段云端开发回测里，`overseas_vote_days=0`，说明海外先验**尚未真正进入正式回测数据**。

因此，现有 V1.8 / V1.9 结果不能表述为“完整海外先验模型”。海外链路是后续必须补齐的独立增量验证项。

### 3.2 PCB 核心篮子宽度

核心思想：观察 PCB / AI 硬件核心票在开盘阶段上涨、下跌、强弱扩散情况，用“篮子宽度”代替只看单一龙头。

具体代码中的核心股票篮子以当前版本配置为准，修改篮子必须记录版本与原因。

### 3.3 PCB 相对上证强弱

不仅看 PCB 本身涨跌，还要看其相对上证指数是否持续走强/走弱。

核心用途：

- 区分“指数带动的普涨”与“PCB 独立强”；
- 区分“指数杀跌”与“PCB 相对抗跌”；
- 为 HIGH_AHEAD / LOW_AHEAD 提供行业相对强弱信息。

### 3.4 开盘前 30 分钟 5 分钟线 / VWAP

观察窗口以 9:30–10:00 为基础。

重点包括：

- 5 分钟 K 线方向与连续性；
- VWAP 上下位置；
- 相对 VWAP 的保持/跌破/回收；
- 早盘高低点是否已经形成；
- 开盘后趋势是否持续扩张。

---

## 4. “开盘情况判断”的正式定义

### 4.1 不再以“收盘涨跌”作为主目标

早期尝试用 10:00 信息预测全天收涨/收跌，准确率只有约 40%–49%，信息价值不足。

后续已改为直接回答与日内高低点任务相关的问题：

> **到 10:00 时，全天高点 / 低点哪一侧更可能还没有出现？**

输出概念：

- `HIGH_AHEAD`：全天高点更可能仍在 10:00 之后；
- `LOW_AHEAD`：全天低点更可能仍在 10:00 之后；
- 其他情况按代码中的中性/混合状态处理。

### 4.2 当前冻结的非对称开盘先验

开发样本：2026 年 1–8 月，按双月区间分段验证。

结论：高点和低点不能使用完全相同的强度阈值。

当前冻结思路：

- `HIGH_AHEAD` 使用更严格的上行票条件，例如“上行票达到较高门槛且净领先明显”；
- `LOW_AHEAD` 使用相对宽松的下行票条件，例如“下行票达到基本门槛且不少于上行票”。

具体数值必须以当前代码实现为准；后续如修改，必须新开版本并更新本文件。

### 4.3 已验证结果

开发期四段总体结果大致为：

- `HIGH_AHEAD` 后续仍出现全天高点：约 **77.3%**；
- `LOW_AHEAD` 后续仍出现全天低点：约 **72.2%**。

完全未参与这轮阈值选择的 2025 年 11–12 月 Holdout：

- `HIGH_AHEAD`：17 天中约 **76.5%** 的全天高点确实在 10:00 后；
- `LOW_AHEAD`：20 天中约 **70.0%** 的全天低点确实在 10:00 后。

这说明“10:00 后哪一侧极值尚未完成”这一目标在样本外基本保持，可作为后续版本的稳定先验层。

---

## 5. 日内高低点候选与确认：当前关键结论

### 5.1 高点与低点不能再共用完全对称的确认逻辑

V1.7 / 早期底层状态机仍存在较强的 HIGH / LOW 对称设计，但样本外表现已经证明两侧行为不同。

2025 年 11–12 月 Holdout 中：

- HIGH 最终确认约有 **37.5% 过早**；
- LOW 最终确认约有 **30.9% 过早**。

因此当前主要矛盾已经从“有没有方向信息”转移到：

> **候选出现后，什么时候才是更接近日高 / 日低的最佳信号阶段？**

### 5.2 HIGH：WATCH 候选更重要

对同一事件比较 WATCH 候选与后续 STRUCTURE_CONFIRM 到真实日高的距离：

- 开发样本中，WATCH 比最终确认更接近日高的比例约 **52.7%**；
- 2025 年 11–12 月 Holdout 中约 **62.5%**。

当前策略方向：

- **HIGH 以 `WATCH_START` / 候选预警为主要实战信号；**
- 后续结构确认更多承担“验证”和“确认高点确实进入衰竭”的作用；
- 不应强求高点一定等到和低点一样的最终确认阶段才输出。

### 5.3 LOW：STRUCTURE_CONFIRM 更重要

同样比较 LOW：

- 开发样本中，STRUCTURE_CONFIRM 比 WATCH 更接近真实日低约 **58.8%**；
- 2025 年 11–12 月 Holdout 中约 **54.5%**。

当前策略方向：

- **LOW 以 `STRUCTURE_CONFIRM` 作为主要低点判断阶段；**
- WATCH 可以作为提前观察，但不能直接等价为可执行低点确认。

### 5.4 当前 V1.9 核心方向

V1.9 不再追求“高低点统一阶段输出”，而采用非对称机制：

- **HIGH = WATCH 候选预警为主 + 后续确认验证；**
- **LOW = STRUCTURE_CONFIRM 为主。**

必须继续对每日全部机械候选统计：

- 候选召回率；
- 真实日高 / 日低覆盖率；
- 每日平均候选数；
- 假阳性负担；
- 候选距真实极值的 bar 距离；
- 候选价格相对真实极值的偏差；
- 确认后 MFE / MAE；
- 过早确认比例；
- 不同日型下的表现。

---

## 6. 评价指标

不要只看“信号后涨跌”。至少同时看：

### 6.1 开盘先验层

- HIGH_AHEAD 命中率；
- LOW_AHEAD 命中率；
- 相对无条件基准提升倍数；
- 分区间最差命中率；
- Holdout 衰减。

### 6.2 候选覆盖层

- 每个交易日是否机械产生候选；
- 真正日高 / 日低是否被候选覆盖；
- 每日候选数量；
- 每个真实极值对应多少候选；
- 候选距真实极值的 5 分钟 bar 数。

### 6.3 执行确认层

- WATCH / STRUCTURE_CONFIRM 谁更接近真实极值；
- 过早 / 过晚比例；
- 确认价到真实日高 / 日低的价格偏差；
- 确认后 30 分钟或指定窗口的 MFE / MAE；
- MFE/MAE 中位数；
- 方向优势；
- 不同开盘日型下的分层结果。

不能只看胜率，必须兼顾候选覆盖、时机距离、MFE/MAE 与假信号数量。

---

## 7. 版本迭代原则

### V1.7

重点：在已有机械状态机基础上优化“什么时候允许执行”。

样本外 2025 年 11–12 月：

- LOW 确认方向优势约 58.2%；
- LOW MFE/MAE 中位数约 1.47；
- HIGH +1/+2 bar 快速确认方向优势约 56.3%；
- HIGH MFE/MAE 约 1.04。

结论：LOW 相对稳定，HIGH 较弱；不能继续统一调阈值。

### V1.8

重点：引入真正的“开盘极值先验”，目标从预测收盘涨跌改为预测高点/低点是否仍在后面。

结论：样本外通过，可保留。

### V1.9

重点：优化极值时机，建立 HIGH / LOW 非对称信号阶段。

当前方向：

- HIGH：WATCH 为主；
- LOW：STRUCTURE_CONFIRM 为主。

继续验证全部机械候选的 recall / precision / 信号负担和极值距离。

---

## 8. 当前待办优先级

### P0：完成 V1.9 全量机械候选汇总

将 2026 年 1–8 月四段结果统一汇总，不能只看单段：

- HIGH WATCH recall / precision；
- LOW STRUCTURE_CONFIRM recall / precision；
- 每日候选数；
- 距真实日高/日低 bar 距离分布；
- 价格偏差；
- 过早/过晚比例；
- 分开盘先验状态统计。

### P0：新的独立 Holdout

V1.9 规则冻结后，用新的未参与开发区间验证，例如 2025 年 9–10 月（如数据完整）。

禁止看完结果后继续用同一区间调 V1.9。

### P1：补齐海外先验数据链

优先把以下变量正式接入日级开盘快照：

- KOSPI；
- Samsung；
- SK Hynix；
- Nikkei；
- SOX / 前夜美股半导体表现。

必须做**增量对照实验**：

- 无海外先验；
- 加海外先验；

比较跨区间和 Holdout 是否有真实增益，不能因为逻辑上“应该有用”就直接保留。

### P1：开盘先验与极值候选联合

开盘先验不直接替代高低点状态机，而应作为条件变量：

- HIGH_AHEAD 日，提高后续 HIGH 候选优先级或持续观察时间；
- LOW_AHEAD 日，提高后续 LOW 确认优先级；
- 不允许开盘先验硬删除所有反向候选，除非机械回测证明不会显著损失真实极值 recall。

---

## 9. 实盘输出原则

模型最终用于盘中时，输出要区分“预警”和“确认”，避免把概率判断说成确定事实。

建议格式：

- 开盘先验：HIGH_AHEAD / LOW_AHEAD / MIXED；
- 高点：WATCH / CONFIRMED；
- 低点：WATCH / CONFIRMED；
- 当前候选时间、价格、score；
- 关键支持变量：PCB 宽度、相对上证强弱、VWAP、海外先验；
- 信号失效条件。

用户偏好：结论尽量简洁、可操作、给量化证据，不堆无关指标。

---

## 10. 后续对话恢复流程

任何新对话继续本项目时，按以下顺序恢复：

1. 读取根目录 `STRATEGY.md`；
2. 查看当前分支最新 commit；
3. 查看最新版本脚本（V1.9 或后续版本）；
4. 查看最近 GitHub Actions 回测日志 / artifacts；
5. 明确当前开发集、验证集、Holdout 是否已经被使用；
6. 在继续调参前先说明本轮要解决的唯一主要问题；
7. 修改规则后同步更新本文件中的“版本迭代”和“当前待办”。

**原则：代码、回测结果和本文件三者必须一致。**

---

最后更新：2026-09-03
