# A股盘中滚动极值概率与T交易模型策略上下文

> 本文件是 `stock_monitor` 的长期策略真源。任何新对话、换模型、换执行环境后，继续本项目之前必须先读本文件，再看最新代码、Actions 与回测结果。不得只凭聊天记忆继续调参。

最后更新：2026-09-03

---

## 1. 项目主目标【当前正式定义】

用户的真实实盘目标不是预测某个固定时点之后涨跌，也不是把 10:00 当作最终判断节点。

**主目标：在盘中每一个可决策时点，滚动判断：**

1. `P_TOP_LOCKED`：截至当前已经形成的日内最高价，后续是否大概率不会再被有效突破；
2. `P_BOTTOM_LOCKED`：截至当前已经形成的日内最低价，后续是否大概率不会再被有效跌破；
3. 当前价格距离已知日内高/低是否仍足够近，避免“判断对了但已经错过交易价”；
4. 从当前价看，剩余上行/下行空间是否形成可执行的做T优势。

**10:00 只是一张滚动快照，不再是主目标或特殊目标时点。**

实盘用途必须拆成两层：

- **极值锁定层**：今天的运行高点/低点是否已经基本形成；
- **T交易价值层**：即使极值已经锁定，当前价格是否仍值得做反T/正T。

反T（先卖后买）至少要求：

`P_TOP_LOCKED高 + 当前价仍接近日高 + 后续下行空间/概率明显优于残余上行风险`

正T（先买后卖）至少要求：

`P_BOTTOM_LOCKED高 + 当前价仍接近日低 + 后续上行空间/概率明显优于残余下行风险`

因此，“是否见顶/见底”和“现在是否值得做T”不得合并成一个信号。

---

## 2. 本轮市场样本口径【2024-09牛市Regime】

### 2.1 Regime起点

当前研究口径正式定义为：

**2024-09-01 之后的数据属于本轮牛市研究样本库。**

原因：用户将 2024 年 9 月视为本轮牛市启动阶段，后续模型应优先在同一大市场 Regime 内研究稳定规律，而不是把大量更早熊市/震荡期数据无条件混入。

### 2.2 使用方式不是“全部混合训练后回看”

2024-09之后的数据可以全部使用，但必须采用**严格时间顺序 walk-forward**：

- 初始训练：2024-09..2024-12；
- 第1段样本外：2025-01..02；
- 再加入2025-01..02，预测2025-03..04；
- 依次扩展；
- 一直到2026-07..08。

当前标准为10个连续双月时间外区间：

1. 2025 Jan-Feb
2. 2025 Mar-Apr
3. 2025 May-Jun
4. 2025 Jul-Aug
5. 2025 Sep-Oct
6. 2025 Nov-Dec
7. 2026 Jan-Feb
8. 2026 Mar-Apr
9. 2026 May-Jun
10. 2026 Jul-Aug

任何一个测试双月的模型都只能看到该双月之前的数据。

### 2.3 “时间顺序样本外”与“完全未见过”必须区分

- walk-forward 能防止**特征/训练数据的未来泄漏**；
- 但如果某个新版本的标签、阈值或结构是看过这些历史结果后设计的，这些历史区间就不再属于严格意义上的“完全 untouched holdout”。

V3.7 的标签修正是在检查 V3.6 历史结果后提出的，因此：

- 2024-09..2026-08 的 V3.7 walk-forward 用于**历史稳健性/跨阶段验证**；
- 真正全新前向样本从 **2026-09 以后**继续累计；
- 不允许因为2026-09以后的实时表现再回头修改同版本阈值后仍称其为 untouched。

---

## 3. 实盘时间轴

### 3.1 盘前 / 集合竞价

程序应在 9:30 前启动。

优先时间点：

- 08:45–09:14：隔夜外围价格、宏观、产业、实时消息；
- 09:15：集合竞价初始状态；
- 09:20：进入不可撤单阶段后的状态；
- 09:24:30–09:24:55：竞价末端重点快照；
- 09:25：最终集合竞价结果，形成 `PREOPEN_PRIOR`。

集合竞价层只是先验，不等价于“已见顶/已见底”。完整竞价模型应使用虚拟参考价、匹配量、未匹配量及 09:15→09:25 的变化路径。

### 3.2 连续竞价

当前基础粒度：**每根完整5分钟K线结束后滚动计算一次**。

09:35、09:40、09:45……直到盘尾，每次重新计算顶部/底部锁定概率与T交易条件。

未来如有稳定1分钟/盘口数据，可以增加更高频确认层，但不能破坏当前机械回放体系。

### 3.3 实盘与历史回放必须共用模型代码

实时 Runner 和历史机械回放不得维护两套独立逻辑。

同一时点、同一输入数据，应得到同一特征、同一概率、同一状态，避免“回测版”和“实盘版”漂移。

---

## 4. 不可违反的回测原则

### 4.1 每日、每时点机械重放

不能只挑“像高低点”的事件。

必须：

- 对研究区间内每一个交易日运行同一程序；
- 对每一个可决策5分钟时点产生状态；
- 所有 HIGH/LOW WATCH/CONFIRM 候选全部保留；
- 不能事后只挑资料最完整或图形最好看的事件。

### 4.2 严禁未来函数

任意时刻 t 的模型输入只能使用 t 及 t 之前真实可获得的数据。

以下只能用于标签/评分，不能作为 t 时点输入：

- 未来全天高/低；
- 后续5分钟线；
- 未来MFE/MAE；
- 未来板块强弱；
- 后来发布的新闻；
- 未来外围价格。

外围消息必须满足：

`published_ts <= decision_ts`

市场确认价格也只能使用在 decision_ts 之前已经真实发生的数据。

### 4.3 不再依赖“单一holdout神话”

当前牛市Regime模型优先采用连续时间顺序 walk-forward，而不是不断把某两个月永久封存导致可用样本越来越少。

评价重点：

- 10段是否方向一致；
- 最差段；
- 样本外衰减；
- 信号覆盖率；
- 每日信号负担；
- 概率校准；
- 不同市场阶段是否出现结构性失效。

### 4.4 禁止看到结果后只调出漂亮胜率

任何阈值变化都必须说明：

1. 为什么调；
2. 使用哪些历史区间决定；
3. 哪些区间只用于评分；
4. 调整后是否增加信号覆盖、校准和T交易价值，而不是只提高一个 pooled precision。

---

## 5. 当前数据层

### 5.1 A股核心价格与板块上下文

核心 PCB 篮子当前以代码配置为准，历史主篮子：

- 002916 深南电路
- 002463 沪电股份
- 600183 生益科技
- 002938 鹏鼎控股
- 603228 景旺电子
- 300476 胜宏科技

同时使用：

- 上证指数；
- PCB宽度；
- PCB相对上证强弱；
- VWAP；
- 量能；
- ATR；
- K线结构；
- WATCH/CONFIRM阶段；
- 日内运行高低点及当前位置。

### 5.2 已确认的长历史5分钟数据

V3.6 云端采集已经验证：

- 区间：2024-09-01..2026-08-31；
- 483个交易日；
- 6只PCB核心股 + 上证指数；
- 每个标的/指数 23,184 根5分钟K；
- 每日48根；
- 缺失交易日0；
- 不完整交易日0；
- 核心数据完整率100%。

因此后续牛市Regime研究可以直接沿用该接口/完整性口径。

### 5.3 外围价格先验

目标包括：

- KOSPI
- Samsung Electronics
- SK Hynix
- Nikkei
- 前夜 SOX
- NVIDIA / 相关AI半导体核心股
- 纳指/相关期货
- 美债收益率
- 美元等风险偏好变量

外围价格是独立先验层，不能和新闻语义分数混成一个不可解释总分。

### 5.4 外围实时消息先验【正式条件，但当前不直接改主模型】

重点覆盖：

- Fed、CPI/PPI、ADP/NFP、就业、美债、美元、降息预期；
- NVIDIA、Broadcom、AMD、Micron、TSMC、AI Server、Hyperscaler CAPEX；
- Samsung、SK Hynix、日本半导体设备/测试链；
- 出口限制、关税、制裁；
- AI/PCB/服务器产业订单、财报、指引、CAPEX；
- 地缘、航运、油价等风险冲击。

结构字段包括：

- `event_direction`
- `event_strength`
- `event_scope`
- `event_freshness`
- `event_confidence`
- `affected_chain`
- `event_source_count`
- `event_active`

当前历史关键词评分只作为数据结构化初筛，**正式模型权重仍视为0，直到增量回测证明稳定贡献。**

正式升级方向：

`事件事实 → actual/consensus surprise → 市场解释 → 产业链传导 → 相关性 → 价格确认 → 历史校准`

---

## 6. 极值锁定标签【V3.7起正式纠正】

### 6.1 V3.6及以前标签存在的问题

旧标签实际比较的是：

`未来最高/最低 vs 当前收盘价`

这会把“已经形成日高、当前价格已经回落，但后面反弹却没有突破原日高”的情况错误标记成“顶部未锁定”。

例如：

- 早盘最高420；
- 当前414；
- 后面反弹417；
- 全天再未超过420。

交易语义上420显然已经成为当天锁定顶部，但旧标签可能因为417相对414仍有上涨而判为未锁定。

**因此旧标签不再作为当前正式定义。**

### 6.2 V3.7顶部锁定正式定义

时点 t 已知：

- `cum_high(t)`：截至t的日内运行最高价；
- `future_high(t)`：t之后实际最高价，仅用于历史标签；
- `tol(t)`：仅使用t时点可知ATR计算的容忍带。

当前容忍带基准：

`tol(t) = max(0.25%, 0.25 × signal-time ATR%)`

正式标签语义：

**`TOP_LOCKED = 后续不再有效突破截至当前已经形成的日内最高价`**

机械表达：

`future_high / cum_high - 1 <= tol(t)`

### 6.3 V3.7底部锁定正式定义

同理：

**`BOTTOM_LOCKED = 后续不再有效跌破截至当前已经形成的日内最低价`**

机械表达：

`cum_low / future_low - 1 <= tol(t)`

### 6.4 “当前位置”不得再污染锁定标签

以下只进入交易价值层，不再进入极值是否已锁定的标签定义：

- 当前close距离日高多少；
- 当前close距离日低多少；
- 从当前close开始未来还能上涨/下跌多少。

正式位置变量：

- `gap_to_high = cum_high / close - 1`
- `gap_to_low = close / cum_low - 1`

极值锁定回答“高/低点是否已经形成”；当前位置回答“现在还能不能以好价格交易”。

---

## 7. T交易价值层【与LOCKED完全分离】

### 7.1 反T

即使 `P_TOP_LOCKED` 很高，也不能自动给反T卖出信号。

需要继续判断：

- 当前是否 near_top；
- 从当前价到未来低点的潜在下行空间；
- 从当前价到未来可能新高的残余上行风险；
- 交易费用/滑点后是否仍有优势。

目标输出逐步升级为：

- `P_REVERSE_T_EDGE`
- `expected_downside`
- `residual_upside_risk`
- `downside/upside ratio`

### 7.2 正T

对称输出：

- `P_POSITIVE_T_EDGE`
- `expected_upside`
- `residual_downside_risk`
- `upside/downside ratio`

### 7.3 典型状态

```text
P_TOP_LOCKED      92%
gap_to_high       2.6%
P_REVERSE_T_EDGE  18%

结论：顶部大概率已形成，但价格已经离高位太远，不再适合反T。
```

反之：

```text
P_TOP_LOCKED      83%
gap_to_high       0.23%
P_REVERSE_T_EDGE  71%

结论：顶部高置信 + 当前仍靠近高位 + 反T具备候选价值。
```

---

## 8. 历史版本结论与当前有效性

### V1.7

用于 HIGH/LOW 候选与确认时机研究。

旧 2025-11..12 结果：

- LOW方向优势约58.2%；
- LOW MFE/MAE中位数约1.47；
- HIGH快速确认方向优势约56.3%。

保留为候选阶段研究，不作为最终滚动概率结论。

### V1.8

旧目标：10:00判断哪一侧日内极值更可能仍在后面。

历史结果：

- 2026-01..08 开发 HIGH_AHEAD ~77.3%；
- LOW_AHEAD ~72.2%；
- 2025-11..12 检查 HIGH_AHEAD ~76.5%；
- LOW_AHEAD ~70.0%。

**保留为开盘先验特征，不再作为主目标。**

### V1.9

候选阶段非对称结论继续保留：

- HIGH：`WATCH_START` 更适合早期高点预警；
- LOW：`STRUCTURE_CONFIRM` 相对更适合低点确认。

这两个阶段作为滚动模型特征，不是最终二元答案。

### V2.0 外围消息层

已具备严格按发布时间附着的结构化能力，但尚未验证出可直接进入正式主模型的稳定增量权重。

### V2.1 集合竞价简化诊断

“09:25最终竞价价 + PCB宽度”简化版约54%，跨期不稳定。

**不晋级正式模型。**

完整09:15→09:25竞价轨迹仍值得继续研究。

### V3.5【旧高精度结论降级】

此前约93%的“高置信锁顶精度”来自LOPO/OOF类历史结构。

在真正时间顺序扩展训练下，早期牛市阶段表现明显下降。

**因此V3.5的93%不再作为正式泛化胜率，不得用于实盘宣传或阈值依据。**

### V3.6【牛市时间顺序基准】

代码/工作流采用：

- 2024-09..12初始训练；
- 2025-01..2026-08共10段双月 expanding walk-forward；
- 无未来训练数据泄漏。

核心结果：

#### TOP

- pooled OOS rows：13,299；
- base rate：19.29%；
- pooled AUC：**0.6267**；
- Brier：0.1483；
- `P_TOP_LOCKED >= 0.60`：23个日级首次锁定信号；
- precision：**47.83%**。

跨期重要现象：

- 2025 Jan-Feb：8次 >=0.60，**0/8正确**；
- 2025 Mar-Apr：3次，**0/3正确**；
- 2026 Jan-Feb：3次，3/3；
- 2026 Mar-Apr：1次，1/1；
- 2026 May-Jun：2次，2/2；
- 2026 Jul-Aug：3次，3/3。

说明固定0.60概率阈值在不同训练阶段**严重失校准**，不能直接作为实盘锁顶阈值。

#### TOP + near high 诊断

在 `P_TOP>=0.60` 且接近已知高位的样本中：

- near-extreme days：12；
- 锁顶正确：11/12 = **91.67%**；
- 但“有利反向空间 > 错误方向空间”仅约 **50%**。

结论：

**判断顶部已锁定 ≠ 当前适合反T。**

#### BOTTOM

- pooled AUC：0.5686；
- >=0.60仅3个信号日；
- precision 0%。

当前底部概率模型明显弱于顶部，不得强行对称处理。

#### 阈值诊断注意事项

V3.6中0.65/0.70出现更高precision，但这些是看完历史后的诊断结果，不是冻结后的独立验证。

尤其：

- 0.70约11个信号日；
- 历史precision约81.8%；
- near-extreme样本9个，历史锁定precision 100%。

**不得因此直接把0.70宣布为生产阈值。**

### V3.7【当前主线】

当前主线已经从“当前价之后还有没有新高/新低”纠正为：

- `P_TOP_LOCKED`：当前运行日高以后是否不再被有效突破；
- `P_BOTTOM_LOCKED`：当前运行日低以后是否不再被有效跌破。

关键变化：

1. 修正标签，不调K线形态参数；
2. 当前价与日高/低的距离从标签中剥离；
3. 锁定概率与T交易价值正式分层；
4. 继续使用2024-09起的10段严格时间顺序walk-forward；
5. V3.7历史结果只能在回测完成后记录，未完成前不得预先宣称提升。

---

## 9. 概率与评价指标【以后主看这些】

### 9.1 概率质量

- AUC；
- Brier Score；
- 概率分桶校准；
- predicted 60/70/80% 与实际命中率差异；
- 不同训练阶段/不同双月的校准漂移。

**禁止只看AUC，也禁止把模型原始 probability 直接当成真实概率。**

### 9.2 顶/底识别

分别统计：

- 日级首次高置信信号数；
- signal-day coverage；
- locked precision；
- 各双月precision；
- 最差双月；
- 过早/过晚；
- 距真实最终极值的5分钟bar距离。

### 9.3 T交易可执行性

反T重点：

- gap_to_high；
- 后续下行空间；
- 残余上行风险；
- downside > upside比例；
- 中位downside/upside ratio；
- 扣除佣金、印花税、滑点后的实际edge。

正T对称统计。

### 9.4 候选阶段

继续统计：

- HIGH WATCH vs STRUCTURE_CONFIRM；
- LOW WATCH vs STRUCTURE_CONFIRM；
- 候选到真实极值的bar距离；
- premature rate；
- MFE/MAE。

### 9.5 跨阶段稳健性

最终模型晋级不能只看 pooled 结果，应至少满足：

- 多数双月方向一致；
- 不存在长期连续失效阶段；
- 最差阶段可接受；
- 信号数不是极少数样本撑出来；
- 校准不会随训练样本增长发生巨大漂移。

---

## 10. 实盘最终输出格式

每根完整5分钟K结束后内部计算一次，但只在状态有意义变化时推送。

建议字段：

```text
10:35  深南电路

P_TOP_LOCKED：83%
P_BOTTOM_LOCKED：18%
运行日高：423.80
当前价：422.90
gap_to_high：0.21%

P_REVERSE_T_EDGE：71%
预计剩余下行：1.05%
残余上行风险：0.24%

阶段：HIGH WATCH
外围价格先验：偏空
实时消息先验：NEUTRAL

结论：顶部高置信候选，当前仍靠近日高，反T可执行性较高。
失效条件：重新放量有效突破运行日高 / PCB宽度重新扩张。
```

必须同时携带：

- `decision_ts`
- `data_ts`
- `last_closed_bar_ts`
- 数据源
- `model_version`
- `signal_id`

---

## 11. 当前优先级

### P0：完成V3.7正确标签的牛市walk-forward

先回答两个问题：

1. 修正为“运行高/低点是否锁定”后，TOP/BOTTOM跨期AUC和precision是否改善；
2. 2025早期牛市与2026强科技阶段是否仍存在明显概率失校准。

### P0：概率校准 / 动态阈值

只有在V3.7标签验证完成后再做。

不允许先为了提高胜率直接把0.60改成0.70。

重点研究：

- expanding-history calibration；
- rolling calibration window；
- 分时段base rate；
- 按训练样本量/市场状态调整置信门槛。

### P0：建立独立T-EDGE层

顶部/底部锁定不能直接触发交易。

需要分别训练/校准：

- `P_REVERSE_T_EDGE`
- `P_POSITIVE_T_EDGE`

### P1：完成V1.9全量候选四段汇总

继续用于HIGH/LOW阶段特征和候选时机研究。

### P1：补齐外围价格历史链

做：

`domestic baseline vs + overseas price prior`

必须是严格按决策时间可得的数据。

### P1：升级外围消息判断

从关键词升级为：

`结构化规则 + LLM语义/传导 + Surprise + 市场价格确认 + 历史增量校准`

但在验证前消息层不得直接改主模型阈值。

### P1：完整集合竞价轨迹

获取并研究：

- 09:15
- 09:20
- 09:24:30~09:24:55
- 09:25

的虚拟价格、匹配量、未匹配量及方向变化。

### P1：实盘Runner

每根完整5分钟K确认收盘后：

1. 读取A股行情；
2. 校验最后完整bar时间；
3. 读取外围价格；
4. 读取决策时间之前的新闻；
5. 计算锁顶/锁底概率；
6. 计算T-EDGE；
7. 只在状态变化时推送。

---

## 12. 新对话恢复流程【必须执行】

1. 先读根目录 `STRATEGY.md`；
2. 查看 `main` 最新 commit；
3. 查看当前最高版本脚本，当前主线为 **V3.7 running-extreme lock walk-forward**；
4. 查看最近 Actions / artifacts；
5. 明确历史区间是“时间顺序OOS”还是“真正完全未见”；
6. 任何调参前先说明本轮唯一主要问题；
7. 规则、标签、阈值、数据口径改变后同步更新本文件；
8. 不得重新把V3.5约93%当正式泛化胜率；
9. 不得把V3.6固定0.60当生产阈值；
10. 不得把`P_TOP_LOCKED/P_BOTTOM_LOCKED`和`P_REVERSE_T_EDGE/P_POSITIVE_T_EDGE`混成一个信号；
11. **牢记：2024-09后属于当前牛市研究Regime；10:00不是最终目标；主目标是盘中每个时点滚动判断运行高/低是否锁定，以及当前位置是否仍有T交易价值。**
