# A股盘中滚动极值概率与T交易模型策略上下文

> 本文件是 `stock_monitor` 的长期策略真源。任何新对话、换模型、换执行环境后，继续本项目之前必须先读本文件，再看最新代码、Actions 与回测结果。不得只凭聊天记忆继续调参。

最后更新：2026-09-03

---

## 1. 项目主目标【2026-09-03重新确认】

用户的真实实盘目标不是预测某个固定时点之后涨跌，也不是把 10:00 作为最终判断节点。

**主目标：在盘中每一个可决策时点，滚动判断：**

1. `P_TOP_LOCKED`：当天顶部是否已经基本锁定 / 后续再出现实质性新高的概率是否已经很低；
2. `P_BOTTOM_LOCKED`：当天底部是否已经基本锁定 / 后续再出现实质性新低的概率是否已经很低；
3. 当前价格距离已知日内高/低是否仍足够近，避免“判断对了但已经错过交易价”；
4. 从当前价看，剩余上行空间与剩余下行空间的概率/期望是否有利于做 T。

**10:00 只是一张滚动快照，不再是主目标或特殊目标时点。**

实盘用途：

- **反T（先卖后买）**：要求 `P_TOP_LOCKED` 较高 + 当前价仍接近高位 + 预期后续下行空间明显大于残余上行空间；
- **正T（先买后卖）**：要求 `P_BOTTOM_LOCKED` 较高 + 当前价仍接近低位 + 预期后续上行空间明显大于残余下行空间。

因此，“是否见顶/见底”和“现在是否值得做T”必须分开输出，不能混为一个信号。

---

## 2. 实盘时间轴

### 2.1 盘前 / 集合竞价

程序应在 9:30 前启动。

优先时间点：

- 08:45–09:14：隔夜外围价格、宏观、产业、实时消息；
- 09:15：集合竞价初始状态；
- 09:20：进入不可撤单阶段后的状态；
- 09:24:30–09:24:55：竞价末端重点快照；
- 09:25：最终集合竞价结果，形成 `PREOPEN_PRIOR`。

集合竞价层是先验，不等价于“已见顶/已见底”。完整竞价模型应使用虚拟参考价、匹配量、未匹配量及 09:15→09:25 的变化路径。

### 2.2 连续竞价

当前基础粒度：**每根完整 5 分钟 K 线结束后滚动计算一次**。

09:35、09:40、09:45……直到盘尾，每次都重新输出顶部/底部概率及 T 交易条件。

未来如有稳定的 1 分钟/盘口数据，可增加更高频确认层，但不能破坏当前机械回放体系。

---

## 3. 不可违反的回测原则

### 3.1 每日、每时点机械重放

不能只挑“像高低点”的事件。

必须：

- 对指定区间内每一个交易日运行同一程序；
- 对每一个可决策 5 分钟时点产生状态；
- 所有 HIGH/LOW WATCH/CONFIRM 候选全部保留；
- 不能事后挑“资料最完整”或“最漂亮”的候选。

### 3.2 严禁未来函数

任意时刻 t 的模型输入只能使用 t 及 t 之前真实可获得的数据。

未来全天高/低、未来 5 分钟线、未来 MFE/MAE、未来板块强弱、后来发布的新闻只能用于评分标签。

外围消息必须按 `published_ts <= decision_ts` 过滤。

### 3.3 开发 / 验证 / Holdout 隔离

- 规则只在开发样本选择；
- 验证集用于比较开发期规则能否稳定；
- untouched holdout 一旦看过，就不能再用于同版本调参；
- 2025-11..12 已经被 V1.7/V1.8/V1.9 检查过，不再视为新版本 untouched holdout；
- 新版本如需真正样本外验证，优先使用此前未参与该版本设计的区间，例如 2025-09..10（数据完整前提下）。

### 3.4 优先稳健，不追求单一区间最高胜率

至少看：

- 多个双月区间方向是否一致；
- 最差区间；
- 样本外衰减；
- 信号覆盖率；
- 每日信号数量；
- 假信号负担；
- 概率校准而非只看胜率。

---

## 4. 当前数据层

### 4.1 A股核心价格与板块上下文

核心 PCB 篮子当前以代码配置为准，历史主篮子：

- 002916 深南电路
- 002463 沪电股份
- 600183 生益科技
- 002938 鹏鼎控股
- 603228 景旺电子
- 300476 胜宏科技

同时使用上证指数、PCB 宽度、PCB 相对指数强弱、VWAP、量能、ATR、K线结构等。

### 4.2 外围价格先验

目标包括：

- KOSPI
- Samsung Electronics
- SK Hynix
- Nikkei
- 前夜 SOX
- 纳指/相关期货
- 美债收益率等风险偏好变量

注意：此前 V1.8 四段开发中 `overseas_vote_days=0`，所以旧结果不能表述为“完整外围模型”。海外价格链仍需独立补齐并做增量验证。

### 4.3 外围实时消息先验【正式条件】

独立于价格先验，重点覆盖：

- Fed、CPI/PPI、ADP/NFP、就业、美债、美元、降息预期；
- NVIDIA、Broadcom、AMD、Micron、TSMC、AI Server、Hyperscaler CAPEX；
- Samsung、SK Hynix、日本半导体设备/测试链；
- 出口限制、关税、制裁；
- AI/PCB/服务器产业订单、财报、指引、CAPEX；
- 地缘、航运、油价等风险冲击。

结构字段包括：

- `event_direction`
- `event_strength`
- `event_scope`
- `event_freshness`
- `event_confidence`
- `affected_chain`
- `event_source_count`
- `event_active`

当前消息层必须和价格层分开评估。历史关键词评分只用于数据结构化初筛，正式实盘应逐步升级为“事件事实 + Surprise + 传导路径 + 板块相关性 + 市场价格确认”。

---

## 5. 滚动极值的正式标签与实盘含义

### 5.1 顶部锁定

在时点 t，使用 t 时刻可知的 ATR 定义一个容忍带。

当前 V2.2 诊断标签：

`top_locked = 后续最高价相对当前可成交价不再高出 max(0.25%, 0.25×ATR%)`

这不是要求精确到一分钱的全天最高价，而是判断“后面还有没有足以让当前反T卖出明显吃亏的新高”。

### 5.2 底部锁定

对称定义：

`bottom_locked = 后续最低价相对当前可成交价不再低出 max(0.25%, 0.25×ATR%)`

### 5.3 可交易性保护

仅判断“顶部已经出现”是不够的。

例如日高 09:50 已出现，但 10:20 已跌 2%，此时顶部锁定概率可能很高，但不应再给反T卖出信号。

因此必须额外检查：

- `near_top`：当前价是否仍接近当日已知高点；
- `near_bottom`：当前价是否仍接近当日已知低点。

### 5.4 T交易质量

最终不能只输出概率，还要同时输出：

- `remaining_upside`：从当前价往后潜在剩余上行空间；
- `remaining_downside`：从当前价往后潜在剩余下行空间；
- 反T：关注 `remaining_downside / remaining_upside`；
- 正T：关注 `remaining_upside / remaining_downside`。

真实交易信号应同时满足“极值概率 + 位置 + 反向空间”。

---

## 6. 已验证的历史结论如何继续使用

### V1.7

用于 HIGH/LOW 候选和确认时机研究。

旧 Holdout 2025-11..12：

- LOW 确认方向优势约 58.2%；
- LOW MFE/MAE 中位数约 1.47；
- HIGH 快速确认方向优势约 56.3%。

### V1.8

旧目标是在 10:00 判断哪一侧日内极值更可能仍在后面。

开发 2026-01..08：

- HIGH_AHEAD ~77.3%；
- LOW_AHEAD ~72.2%。

旧 Holdout 2025-11..12：

- HIGH_AHEAD ~76.5%；
- LOW_AHEAD ~70.0%。

**V1.8 结果保留，但从现在起只作为滚动模型中的一个“开盘先验特征”，不再是主目标。**

### V1.9

核心非对称结论仍保留：

- HIGH：`WATCH_START` 比等到最终确认更适合做早期高点预警；
- LOW：`STRUCTURE_CONFIRM` 相对更适合做低点确认。

这两个阶段将作为 V2.2 及后续滚动概率模型的重要特征，而不是最终二元答案。

### V2.0 外围消息层

消息层已结构化并能按决策时间过滤，但尚未取得正式模型权重。必须做增量对照后再晋级。

### V2.1 09:25 最终竞价价诊断

2026-01..08 的“09:25最终竞价价 + PCB宽度”简化版总体胜率约 54%，验证期不稳定。

**结论：不晋级正式模型。**

该结果只说明“最终竞价价本身”不足，不能否定完整 09:15→09:25 竞价轨迹的价值。下一步竞价研究应补充虚拟价格、匹配量、未匹配量及其变化。

### V2.2 滚动极值概率【当前主线】

当前代码：

- `market_data/rolling_extreme_probability_v22.py`
- `market_data/rolling_extreme_probability_v22_cloud.py`
- `.github/workflows/rolling-extreme-probability-v22.yml`

目标：

- 每个完整 5m bar 输出 `P_TOP_LOCKED` / `P_BOTTOM_LOCKED`；
- 概率由开发样本经验校准，验证集只评分，不参与拟合；
- 同时评估 near_top/near_bottom、剩余反向空间及错误方向空间；
- 当前先做国内价格上下文 baseline，外围消息/海外价格/完整竞价层后续逐层做增量验证。

---

## 7. 评价指标【以后主看这些】

### 7.1 概率质量

- Brier Score；
- AUC；
- 概率分桶校准：预测 70% 的样本实际是否接近 70%；
- 各时段概率基准差异。

### 7.2 顶/底识别

在 `P>=60% / 70% / 80%` 时分别统计：

- 信号数；
- 信号天数；
- 实际 top/bottom locked precision；
- 每天平均信号负担。

### 7.3 T交易可执行性

反T：

- 信号时是否 near_top；
- 后续下行空间是否大于残余上行空间；
- 中位后续下行空间；
- 中位错误方向上行空间；
- reversal / residual ratio。

正T对称统计。

### 7.4 候选阶段

继续统计：

- HIGH WATCH vs STRUCTURE_CONFIRM；
- LOW WATCH vs STRUCTURE_CONFIRM；
- 候选距真实极值的 bar 距离；
- 过早/过晚；
- MFE/MAE。

---

## 8. 实盘最终输出格式

每次滚动计算应尽量简洁，例如：

```text
10:35  深南电路
顶部锁定概率：78%
底部锁定概率：21%
当前位置：距日内高点 0.18%，仍属 near_top
预计剩余上行：0.25%
预计剩余下行：1.10%
反T质量：高
状态：HIGH WATCH / 尚未结构确认

关键证据：PCB宽度转弱、相对上证走弱、跌破短线结构、VWAP位置、外围价格、实时消息
失效条件：重新放量创新高 / 板块宽度重新扩张
```

程序每 5 分钟计算，但只在状态发生有意义变化时推送，避免重复刷屏。

---

## 9. 当前优先级

### P0

完成 V2.2 滚动极值概率验证，先确认国内价格/板块 baseline 是否具备概率校准能力。

### P0

完成 V1.9 全量机械候选四段汇总，继续为 V2.2 提供 HIGH/LOW 阶段特征。

### P1

补齐完整海外价格历史链，并做 baseline vs +overseas 增量验证。

### P1

升级外围消息判断：从关键词升级为事件语义/Surprise/传导/市场确认，并做严格按时间戳的历史回放。

### P1

获取完整历史集合竞价过程：09:15、09:20、09:24:30~09:24:55、09:25 的虚拟价、匹配量、未匹配量及方向。

### P1

实盘 Runner：每根完整 5 分钟 K 后自动读取行情、外围、消息，计算滚动概率并仅在状态变化时推送。

---

## 10. 新对话恢复流程

1. 先读本文件 `STRATEGY.md`；
2. 查看 `main` 最新 commit；
3. 查看当前最高版本脚本，当前主线为 V2.2 rolling extreme probability；
4. 查看最近 Actions / artifacts；
5. 明确开发、验证、holdout 是否已经被使用；
6. 任何调参前先说明本轮唯一主要问题；
7. 规则/目标改变后同步更新本文件；
8. **牢记：10:00不是最终目标，主目标是盘中每个时点滚动判断见顶/见底概率及T交易质量。**
